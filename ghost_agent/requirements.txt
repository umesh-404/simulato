# Ghost Agent dependencies
# These are compiled into the .exe by PyInstaller.
# Install on the BUILD machine only (not needed on the exam laptop).
dxcam>=0.3.0
numpy>=1.24.0
opencv-python-headless>=4.8.0
pyinstaller>=6.0.0
